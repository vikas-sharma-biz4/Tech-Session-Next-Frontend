// Server-side translations
import { en } from './en';

export const serverTranslations = {
  en,
};

export type ServerLocale = 'en';

export const defaultServerLocale: ServerLocale = 'en';

