// Client-side translations
export const clientTranslations = {
  en: {
    // Add client-side translations here
  },
};

export type Locale = 'en';

export const defaultLocale: Locale = 'en';

